export interface Product {
  id: string;
  producer_id: string;
  category: string;
  price: number;
  description: string;
  stock: number;
  unit: string;
  photo_url: string;
  create_at: string;
}